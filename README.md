# Project Alliance

**AI Co-scientist: 연구의 전체 사이클을 관장하는 멀티 에이전트 시스템**

---

## 개요

Project Alliance는 연구자의 연구 과정 전체를 지원하는 AI 공동연구자(AI Co-scientist) 시스템입니다. 아이디어 발굴부터 논문 출판까지 연구의 모든 단계를 관장하며, 각 단계별로 전문화된 하위 에이전트들이 협력하여 연구를 수행합니다.

### 핵심 가치
- **Human-in-the-Loop**: 연구자가 언제든 개입하여 방향을 수정할 수 있음
- **모듈화된 에이전트**: 각 연구 단계별 전문 에이전트로 구성
- **확장 가능한 구조**: 새로운 에이전트 추가 및 워크플로우 커스터마이징 용이
- **플랫폼 독립성**: Claude Code에서 시작하여 Google ADK로 마이그레이션 가능

---

## 시스템 아키텍처 다이어그램

```
┌─────────────────────────────────────────────────────────────────────────────────┐
│                                 USER / 연구자                                    │
│                         (연구 주제 입력, 승인/피드백 제공)                          │
└──────────────────────────────────────┬──────────────────────────────────────────┘
                                       │
                                       ▼
┌─────────────────────────────────────────────────────────────────────────────────┐
│                              Alliance 클래스                                     │
│                              (src/index.ts)                                     │
│  ┌─────────────┬─────────────┬─────────────┬─────────────┬─────────────┐       │
│  │  Config     │   State     │  Workflow   │   Agent     │ Intervention│       │
│  │  Loader     │   Manager   │   Engine    │   Registry  │   Manager   │       │
│  └──────┬──────┴──────┬──────┴──────┬──────┴──────┬──────┴──────┬──────┘       │
│         │             │             │             │             │              │
│         ▼             ▼             ▼             ▼             ▼              │
│    ┌─────────┐  ┌──────────┐  ┌──────────┐  ┌──────────┐  ┌──────────┐        │
│    │  YAML   │  │ Session  │  │Sequential│  │ 6 Agent  │  │ Approval │        │
│    │ configs │  │  State   │  │ Parallel │  │ Instances│  │  Gates   │        │
│    │         │  │          │  │   Loop   │  │          │  │          │        │
│    └─────────┘  └──────────┘  └──────────┘  └──────────┘  └──────────┘        │
└─────────────────────────────────────────────────────────────────────────────────┘
```

### 연구 워크플로우 실행 흐름

```
  연구 주제 입력
       │
       ▼
┌──────────────────┐        outputKey: research_idea
│  1. IDEA         │───────────────────────────────────────┐
│     BUILDING     │  아이디어 빌딩                         │
│                  │  - 연구 갭 식별, 가설 수립              │
└────────┬─────────┘                                       │
         │                                                 │
         ▼                                                 │
┌──────────────────┐        outputKey: literature_results  │
│  2. LITERATURE   │───────────────────────────────────┐   │
│     SEARCH       │  문헌검색                          │   │
│                  │  - 관련 논문 검색, 인용 분석        │   │
└────────┬─────────┘                                   │   │
         │                                             │   │
         ▼                                             ▼   ▼
┌──────────────────┐        outputKey: experiment_design
│  3. EXPERIMENT   │◀────(의존: research_idea, literature_results)
│     DESIGN       │  실험설계
│                  │  - 변수 정의, 프로토콜 작성
└────────┬─────────┘
         │
    ┌────┴────┐
    │ 🔒 승인  │  ◀─── Human-in-the-Loop
    │  게이트  │       연구자가 실험 설계 검토/승인
    └────┬────┘
         │ approved
         ▼
┌──────────────────┐        outputKey: analysis_results
│  4. DATA         │◀────(의존: experiment_design)
│     ANALYSIS     │  데이터분석
│                  │  - 통계 분석, 시각화
└────────┬─────────┘
         │
         ▼
┌──────────────────┐        outputKey: paper_draft
│  5. PAPER        │◀────(의존: 모든 이전 결과)
│     WRITING      │  논문쓰기
│                  │  - 섹션별 작성
└────────┬─────────┘
         │
    ┌────┴────┐
    │ 🔒 승인  │  ◀─── Human-in-the-Loop
    │  게이트  │       연구자가 논문 초안 검토/수정
    └────┬────┘
         │ approved
         ▼
┌──────────────────┐        outputKey: final_document
│  6. FORMATTING   │◀────(의존: paper_draft)
│     REVIEW       │  포맷팅 검토
│                  │  - 저널 형식 검토, 최종 교정
└────────┬─────────┘
         │
    ┌────┴────┐
    │ 🔒 승인  │  ◀─── Human-in-the-Loop
    │  게이트  │       연구자가 최종 문서 확인
    └────┬────┘
         │ approved
         ▼
    ┌─────────┐
    │  완료    │  WorkflowResult 반환
    └─────────┘
```

### 에이전트 간 상태 공유 메커니즘

```
┌─────────────────────────────────────────────────────────────────────────────────┐
│                              SessionState (공유 상태)                            │
│  ┌───────────────────────────┬───────────────────────────┬─────────────────┐   │
│  │  Key                      │  Value                    │  Written By     │   │
│  ├───────────────────────────┼───────────────────────────┼─────────────────┤   │
│  │  research_idea            │  { hypothesis, gaps }     │  IdeaBuilding   │   │
│  │  literature_results       │  { papers, citations }    │  LitSearch      │   │
│  │  experiment_design        │  { variables, protocol }  │  ExpDesign      │   │
│  │  analysis_results         │  { stats, visuals }       │  DataAnalysis   │   │
│  │  paper_draft              │  { sections, abstract }   │  PaperWriting   │   │
│  │  final_document           │  { formatted, issues }    │  Formatting     │   │
│  └───────────────────────────┴───────────────────────────┴─────────────────┘   │
└─────────────────────────────────────────────────────────────────────────────────┘
         ▲                              │
         │ state.set(outputKey, data)   │ state.get('key')
         │                              ▼
┌─────────────────────────────────────────────────────────────────────────────────┐
│   ┌─────────────┐    ┌─────────────┐    ┌─────────────┐    ┌─────────────┐     │
│   │   Agent 1   │───▶│   Agent 2   │───▶│   Agent 3   │───▶│   Agent N   │     │
│   └─────────────┘    └─────────────┘    └─────────────┘    └─────────────┘     │
│                                                                                 │
│   instruction 템플릿: "Research idea: {research_idea}" → 값 치환                 │
└─────────────────────────────────────────────────────────────────────────────────┘
```

### 오케스트레이션 패턴

```
1. SEQUENTIAL (현재 기본 워크플로우)
   ┌───┐    ┌───┐    ┌───┐    ┌───┐    ┌───┐    ┌───┐
   │ A │───▶│ B │───▶│ C │───▶│ D │───▶│ E │───▶│ F │
   └───┘    └───┘    └───┘    └───┘    └───┘    └───┘

2. PARALLEL (예: 여러 DB 동시 검색)
                    ┌───┐
               ┌───▶│ B │───┐
   ┌───┐       │    └───┘   │    ┌───────┐    ┌───┐
   │ A │───────┼───▶┌───┐───┼───▶│Merger │───▶│ D │
   └───┘       │    │ C │   │    └───────┘    └───┘
               └───▶└───┘───┘

3. LOOP (예: 논문 수정 사이클)
   ┌───────────────────────────────┐
   │         ┌───┐    ┌───┐       │  max iterations 또는
   │    ┌───▶│ A │───▶│ B │───┐   │  조건 충족 시 종료
   │    └────┴───┴────┴───┴───┘   │
   └──────────────┬───────────────┘
                  ▼ [EXIT]

4. HYBRID (중첩 가능)
   ┌───┐    ┌─────────────────┐    ┌───┐
   │ A │───▶│  Parallel Block │───▶│ E │
   └───┘    │  ┌───┐  ┌───┐   │    └───┘
            │  │ B │  │ C │   │
            └──┴───┴──┴───┴───┘
```

### Human-in-the-Loop 상세

```
    Agent 실행 완료
          │
          ▼
    ┌───────────────────┐
    │ approvalGates에    │───No──▶ 다음 Agent로 진행
    │ 포함된 Agent인가?  │
    └─────────┬─────────┘
              │ Yes
              ▼
    ┌───────────────────┐      ┌────────────────────────────────┐
    │ InterventionMgr   │◀────│  CLI: ConsoleInterventionHandler │
    │ .requestApproval()│      │  - 콘솔에 출력 표시              │
    └─────────┬─────────┘      │  - 사용자 입력 대기 (yes/no/edit)│
              │                └────────────────────────────────┘
              ▼
    ┌───────────────────┐
    │   HumanResponse   │
    │ { approved, feedback, modifications }
    └─────────┬─────────┘
              │
       ┌──────┴──────┐
       ▼             ▼
   approved       rejected
       │             │
       ▼             ▼
  다음 Agent     워크플로우
    진행           중단
```

### 현재 구현 상태

```
┌─────────────────┬──────────────────────────────────────────────────────────┐
│     구성요소     │                        상태                              │
├─────────────────┼──────────────────────────────────────────────────────────┤
│ 핵심 인터페이스  │ ✅ 완료 (IAgent, IWorkflow, ISessionState, etc.)         │
│ 오케스트레이션   │ ✅ 완료 (Sequential, Parallel, Loop, WorkflowEngine)     │
│ 상태 관리       │ ✅ 완료 (StateManager, SessionState)                     │
│ HITL 시스템     │ ✅ 완료 (InterventionManager, ApprovalGate)              │
│ 설정 시스템     │ ✅ 완료 (ConfigLoader, YAML schemas)                     │
│ 6개 에이전트    │ ⚠️ 스켈레톤만 (skills 미정의, LLM 미연동)                 │
│ Tools          │ ❌ 미구현 (문헌검색, 통계분석 등)                          │
│ LLM 연동       │ ❌ 미구현 (Claude API 연동 필요)                          │
└─────────────────┴──────────────────────────────────────────────────────────┘
```

---

## 기술 스택
- **언어**: TypeScript / Node.js
- **런타임**: Node.js 18+
- **패키지 매니저**: npm
- **주요 의존성**: Zod (스키마 검증), YAML (설정 파싱), UUID

### 프로젝트 구조
```
alliance/
├── src/
│   ├── index.ts                     # 메인 진입점 + Alliance 클래스
│   ├── core/
│   │   ├── interfaces/              # 핵심 인터페이스 정의
│   │   │   ├── agent.interface.ts   # IAgent, IWorkflowAgent
│   │   │   ├── workflow.interface.ts # IWorkflow, WorkflowContext
│   │   │   ├── state.interface.ts   # ISessionState, IStateManager
│   │   │   └── tool.interface.ts    # ITool, IToolRegistry
│   │   ├── types/                   # 타입 및 스키마 정의
│   │   │   ├── agent.types.ts       # AgentContext, AgentResult
│   │   │   └── workflow.types.ts    # ResearchStage, StateKeys
│   │   └── base/
│   │       └── BaseAgent.ts         # 모든 에이전트의 추상 베이스 클래스
│   ├── agents/
│   │   ├── registry/
│   │   │   └── AgentRegistry.ts     # 에이전트 등록 및 조회
│   │   └── research/                # 6개 연구 에이전트
│   │       ├── IdeaBuildingAgent.ts
│   │       ├── LiteratureSearchAgent.ts
│   │       ├── ExperimentDesignAgent.ts
│   │       ├── DataAnalysisAgent.ts
│   │       ├── PaperWritingAgent.ts
│   │       └── FormattingReviewAgent.ts
│   ├── orchestration/
│   │   ├── WorkflowEngine.ts        # 워크플로우 실행 엔진
│   │   └── workflows/
│   │       ├── SequentialWorkflow.ts  # 순차 실행
│   │       ├── ParallelWorkflow.ts    # 병렬 실행
│   │       └── LoopWorkflow.ts        # 반복 실행
│   ├── state/
│   │   └── StateManager.ts          # 세션 및 상태 관리
│   ├── human-in-loop/
│   │   ├── InterventionManager.ts   # 사용자 개입 관리
│   │   └── ApprovalGate.ts          # 승인 게이트 설정
│   ├── config/
│   │   └── ConfigLoader.ts          # YAML 설정 로더
│   └── utils/
│       └── logger.ts                # 로깅 유틸리티
├── config/
│   ├── agents/                      # 에이전트 설정 (YAML)
│   │   ├── idea-building.yaml
│   │   ├── literature-search.yaml
│   │   ├── experiment-design.yaml
│   │   ├── data-analysis.yaml
│   │   ├── paper-writing.yaml
│   │   └── formatting-review.yaml
│   └── workflows/
│       └── default-research.yaml    # 기본 연구 워크플로우
├── dist/                            # 컴파일된 JavaScript
├── package.json
└── tsconfig.json
```

---

## 연구 워크플로우

### 기본 6단계 연구 파이프라인

```
┌─────────────────┐     ┌─────────────────┐     ┌─────────────────┐
│  Idea Building  │────▶│ Literature      │────▶│   Experiment    │
│  아이디어 빌딩   │     │ Search 문헌검색  │     │  Design 실험설계 │
└─────────────────┘     └─────────────────┘     └────────┬────────┘
                                                         │
                                                    [승인 게이트]
                                                         │
                                                         ▼
┌─────────────────┐     ┌─────────────────┐     ┌─────────────────┐
│   Formatting    │◀────│  Paper Writing  │◀────│  Data Analysis  │
│ Review 포맷팅   │     │  논문쓰기        │     │  데이터분석      │
└────────┬────────┘     └────────┬────────┘     └─────────────────┘
         │                       │
    [승인 게이트]            [승인 게이트]
         │                       │
         ▼                       │
    [완료/출판]◀─────────────────┘
```

### 하위 에이전트 상세

| 순서 | Agent ID | 한국어 | 영어 | 역할 | Output Key |
|:---:|----------|--------|------|------|------------|
| 1 | `idea-building` | 아이디어 빌딩 | Idea Building | 연구 주제 탐색, 가설 수립, 연구 갭 발굴 | `research_idea` |
| 2 | `literature-search` | 문헌검색 | Literature Search | 관련 논문 검색, 인용 분석, 문헌 정리 | `literature_results` |
| 3 | `experiment-design` | 실험설계 | Experiment Design | 연구 방법론 설계, 변수 정의, 프로토콜 작성 | `experiment_design` |
| 4 | `data-analysis` | 데이터분석 | Data Analysis | 통계 분석, 시각화, 결과 해석 | `analysis_results` |
| 5 | `paper-writing` | 논문쓰기 | Paper Writing | 논문 초안 작성, 섹션별 구성 | `paper_draft` |
| 6 | `formatting-review` | 포맷팅 검토 | Formatting Review | 저널 형식 검토, 인용 확인, 최종 교정 | `final_document` |

### 에이전트 간 데이터 흐름

각 에이전트는 `outputKey`를 통해 결과를 공유 상태(SessionState)에 저장하고, 다음 에이전트는 이를 `{key}` 템플릿 문법으로 참조합니다.

```typescript
// 예: paper-writing 에이전트의 instruction
instruction: |
  Research idea: {research_idea}
  Literature: {literature_results}
  Experiment design: {experiment_design}
  Analysis results: {analysis_results}
```

---

## 오케스트레이션 패턴

### 1. Sequential (순차 실행)
기본 연구 워크플로우. 에이전트 A → B → C 순서로 실행.

```typescript
const workflow = new SequentialWorkflow('research', 'Research', [
  ideaBuildingAgent,
  literatureSearchAgent,
  experimentDesignAgent,
  // ...
]);
```

### 2. Parallel (병렬 실행)
독립적인 작업을 동시에 수행. Fan-out → Gather 패턴.

```typescript
const workflow = new ParallelWorkflow('parallel-lit', 'Parallel Literature', [
  pubmedSearchAgent,
  arxivSearchAgent,
  googleScholarAgent,
], config, mergerAgent);  // 결과를 합치는 merger 에이전트
```

### 3. Loop (반복 실행)
조건이 충족될 때까지 반복. 논문 수정/리뷰 사이클에 적합.

```typescript
const workflow = new LoopWorkflow('revision', 'Revision Cycle', [
  reviewerAgent,
  revisionAgent,
], { maxIterations: 5 }, terminationCondition);
```

### 4. Hybrid (하이브리드)
Sequential 기반으로 Parallel/Loop 워크플로우를 중첩하여 복잡한 파이프라인 구성.

---

## Human-in-the-Loop (HITL)

### 승인 게이트 (Approval Gates)

연구의 중요한 전환점에서 사용자 확인을 요청합니다.

| 게이트 위치 | 목적 |
|------------|------|
| `experiment-design` 완료 후 | 실험 시작 전 설계 검토 |
| `paper-writing` 완료 후 | 출판 전 내용 검토 |
| `formatting-review` 완료 후 | 최종 제출 전 확인 |

### 개입 방식

```typescript
// 승인 요청 처리
const response = await hitl.requestApproval({
  agentId: 'experiment-design',
  stage: 'experiment_design',
  type: 'output',
  summary: 'Review experiment design before proceeding',
  content: experimentDesignOutput,
});

if (!response.approved) {
  // 사용자가 거부하면 워크플로우 중단
}

if (response.modifications) {
  // 사용자가 수정 사항을 제공하면 반영
}
```

### InterventionHandler 인터페이스

CLI, Web UI, API 등 다양한 인터페이스와 연동 가능:

```typescript
interface InterventionHandler {
  handleApproval(request: ApprovalRequest): Promise<HumanResponse>;
  collectFeedback(prompt: string, context: FeedbackContext): Promise<string>;
  notify?(notification: NotificationData): Promise<void>;
}
```

---

## 설정 시스템

### 에이전트 설정 (YAML)

`config/agents/idea-building.yaml` 예시:

```yaml
id: idea-building
name: IdeaBuildingAgent
displayName:
  en: Idea Building
  ko: 아이디어 빌딩

description: |
  Helps researchers brainstorm and develop research ideas.

instruction: |
  You are a Research Idea Development Specialist...

tools: []

skills: []  # 사용자가 나중에 정의

outputKey: research_idea

timeout: 600000  # 10분

requiresApproval: false

dependencies: []

version: "1.0.0"
tags:
  - research
  - ideation
```

### 워크플로우 설정 (YAML)

`config/workflows/default-research.yaml`:

```yaml
id: default-research
name: Default Research Workflow
type: sequential

agents:
  - id: idea-building
  - id: literature-search
  - id: experiment-design
  - id: data-analysis
  - id: paper-writing
  - id: formatting-review

config:
  continueOnError: false
  approvalGates:
    - experiment-design
    - paper-writing
    - formatting-review
```

### Skills 정의

각 에이전트의 세부 능력은 `skills` 배열에 정의합니다:

```yaml
skills:
  - name: domain_analysis
    description: Analyze research domain landscape
    enabled: true
    config:
      depth: deep

  - name: gap_identification
    description: Identify research gaps and opportunities
    enabled: true

  - name: hypothesis_generation
    description: Generate testable hypotheses
    enabled: true
```

---

## 상태 관리

### SessionState

연구 세션 전체에 걸쳐 유지되는 공유 상태:

```typescript
interface ISessionState {
  sessionId: string;
  currentStage: ResearchStage;
  researchTopic: string;

  get<T>(key: string): T | undefined;
  set<T>(key: string, value: T): void;
  has(key: string): boolean;
}
```

### StateKeys (표준 키)

에이전트 간 데이터 전달을 위한 표준화된 키:

```typescript
const StateKeys = {
  RESEARCH_IDEA: 'research_idea',
  LITERATURE_RESULTS: 'literature_results',
  EXPERIMENT_DESIGN: 'experiment_design',
  ANALYSIS_RESULTS: 'analysis_results',
  PAPER_DRAFT: 'paper_draft',
  FINAL_DOCUMENT: 'final_document',

  temp: (key: string) => `temp:${key}`,  // 현재 턴에서만 유효
};
```

### 영속성

`IStatePersistenceAdapter` 인터페이스를 통해 다양한 스토리지 지원:
- `MemoryPersistenceAdapter` (기본, 인메모리)
- 파일 시스템, 데이터베이스 등 확장 가능

---

## 사용법

### 설치

```bash
npm install
npm run build
```

### 기본 사용

```typescript
import { Alliance } from 'alliance';

const alliance = new Alliance({
  configDir: './config',
  logLevel: 'info',
  autoApprove: false,  // true로 설정하면 승인 게이트 자동 통과 (테스트용)
});

await alliance.initialize();

const result = await alliance.runResearchWorkflow(
  'Effects of sleep deprivation on cognitive performance'
);

console.log(result.success);
console.log(result.output);
```

### 커스텀 워크플로우 실행

```typescript
const result = await alliance.runWorkflow('my-custom-workflow', {
  topic: 'Custom research topic',
  constraints: { ... }
});
```

### 컴포넌트 직접 접근

```typescript
const registry = alliance.getAgentRegistry();
const stateManager = alliance.getStateManager();
const hitl = alliance.getInterventionManager();
const engine = alliance.getWorkflowEngine();
```

---

## 확장 가이드

### 새 에이전트 추가

1. `src/agents/research/` 에 새 에이전트 클래스 생성:

```typescript
import { BaseAgent } from '../../core/base/BaseAgent.js';

export class MyNewAgent extends BaseAgent {
  protected async runAgent(context: AgentContext, instruction: string): Promise<unknown> {
    // 에이전트 로직 구현
  }

  protected generateSummary(output: unknown): string {
    return 'Summary of output';
  }
}
```

2. `config/agents/` 에 YAML 설정 추가

3. `src/agents/research/index.ts` 에서 export

4. `src/index.ts` 의 factories에 등록

### 새 Tool 추가

```typescript
import { ITool } from '../core/interfaces/tool.interface.js';
import { z } from 'zod';

const myTool: ITool<MyInput, MyOutput> = {
  name: 'my_tool',
  description: 'Does something useful',
  inputSchema: z.object({ ... }),

  async execute(input, context) {
    // Tool 로직
    return result;
  }
};
```

---

## Google ADK 마이그레이션

이 프로젝트는 Google ADK(Agent Development Kit)와 호환되도록 설계되었습니다:

| Alliance | Google ADK |
|----------|------------|
| `IAgent` | `LlmAgent` |
| `SequentialWorkflow` | `SequentialAgent` |
| `ParallelWorkflow` | `ParallelAgent` |
| `LoopWorkflow` | `LoopAgent` |
| `SessionState` | `Session.state` |
| `outputKey` | `outputKey` |
| `actions.escalate` | `toolContext.actions.escalate` |

`src/providers/` 디렉토리에 `ADKProvider`를 구현하여 전환 가능합니다.

---

## 개발 로드맵

### Phase 1: 초기 구축 ✅
- [x] 프로젝트 구조 설정
- [x] 핵심 인터페이스 정의
- [x] 오케스트레이션 엔진 구현
- [x] Human-in-the-Loop 시스템
- [x] 6개 연구 에이전트 스켈레톤
- [x] YAML 설정 시스템

### Phase 2: Skills 정의 (예정)
- [ ] 각 에이전트별 세부 skills 정의
- [ ] LLM 연동 (Claude API)
- [ ] 실제 연구 수행 로직 구현

### Phase 3: Tools 확장 (예정)
- [ ] 문헌 검색 도구 (PubMed, arXiv, Google Scholar)
- [ ] 통계 분석 도구
- [ ] 시각화 도구
- [ ] 인용 관리 도구

### Phase 4: 인터페이스 (예정)
- [ ] CLI 인터페이스 개선
- [ ] Web UI 대시보드
- [ ] API 서버

### Phase 5: 고급 기능 (예정)
- [ ] 멀티 프로젝트 관리
- [ ] 협업 기능
- [ ] 버전 관리 및 히스토리
- [ ] Google ADK 마이그레이션

---

## 라이선스

MIT License

---

## 기여

이슈와 PR을 환영합니다. 기여 전 이슈를 먼저 열어 논의해 주세요.
